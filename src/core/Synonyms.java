package core;

/**
 * This class will be used to find all the synonyms after one of our request
 * We need to ask HPO database for our symptom's synonyms and then doing the research in each database for each synonyms (and the original symptom). The synonyms' information
 * could be checked with drug bank for example.
 */
public class Synonyms {
}
